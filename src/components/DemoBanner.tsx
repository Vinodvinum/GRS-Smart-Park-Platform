export function DemoBanner() {
  return (
    <div className="demoBanner" role="note">
      <span className="demoDot" aria-hidden="true" />
      Demo environment · Simulated data only · No live GRS systems connected
    </div>
  );
}
